# uv init
# uv add fastapi uvicorn
-------
# uv run uvicorn main:app --reload
# uv run uvicorn main:app --host 0.0.0.0 --port 5000  --workers 2 
# uv run uvicorn main:app --host 0.0.0.0 --port 5000  --reload
# uv run uvicorn main:app --env-file .env
-------
Client
   │
   │ GET /about
   ▼
Uvicorn
   │
   │ transmet la requête à l'application
   ▼
FastAPI
   │
   │ cherche une route correspondant à
   │ GET + /about
   ▼
@app.get("/about")
def about():
   │
   │ exécution
   ▼
Python dict
   │
   │ {"name": ..., "age": ..., "job": ...}
   ▼
FastAPI
   │
   │ sérialisation
   ▼
Réponse HTTP JSON
   │
   ▼
Client
---------------
# Query & Path
Si la valeur sert à identifier directement une ressource, on utilise généralement le Path. Si elle sert à filtrer, rechercher, trier ou contrôler la manière dont on récupère une collection, on utilise généralement Query.
---------------
# Header
Un header est une information supplémentaire envoyée avec la requête HTTP

* Accept
Indique au serveur quel type de réponse le client souhaite.
-> Accept: application/json

* Content-Type
Indique le format des données envoyées dans le body.
-> Content-Type: application/json

* Authorization
Sert généralement à transmettre des informations d'authentification.
-> Authorization: Bearer eyJhbGciOi...

* User-Agent
Permet généralement d'indiquer quel client effectue la requête
-> User-Agent: Mozilla/5.0
--> user_agent: str = Header() # forcer le header
--> user_agent: str | None = Header(default=None) # le header peut être nul
--> user_agent: str | None = Header(default=None, alias="User-Agent")
-----------------
# le Body avec pydantic
Client
  │
  │ JSON
  ▼
FastAPI
  │
  ▼
Pydantic
  │
  │ validation
  ▼
UserCreate
  │
  ▼
create_user(user)
  │
  ▼
réponse JSON
-------------------
                 REQUÊTE HTTP
                      │
       ┌──────────────┼──────────────┐
       │              │              │
      Path           Query         Header
       │              │              │
       ▼              ▼              ▼
 /users/25     ?limit=10       X-Client: App
       │              │              │
       └──────────────┼──────────────┘
                      │
                      ▼
                    FastAPI
                      │
                      ▼
                    Body
                      │
                      ▼
             Pydantic BaseModel
----------------
# les erreurs 422
type
 ↓
nature du problème

loc
 ↓
où est le problème ?

msg
 ↓
explication humaine

input
 ↓
valeur reçue
--------------------------
                    CLIENT
                       │
                       │ HTTP
                       ▼
                 ┌───────────┐
                 │  Uvicorn  │
                 └─────┬─────┘
                       │
                       ▼
                 ┌───────────┐
                 │  FastAPI  │
                 └─────┬─────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
        Path         Query        Header
          │            │            │
          └────────────┼────────────┘
                       │
                      Body
                       │
                       ▼
                   Pydantic
                       │
                  validation
                       │
                ┌──────┴──────┐
                │             │
                ❌             ✅
                │             │
               422          fonction
                              │
                              ▼
                         réponse JSON
----------------------
                    CLIENT
                       │
                       │ HTTP Request
                       ▼
              ┌─────────────────┐
              │     FastAPI     │
              └─────────────────┘
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
      Path           Query          Header
        │              │              │
        └──────────────┼──────────────┘
                       │
                       ▼
                  Request Body
                       │
                       ▼
                Pydantic Model
                       │
                       ▼
                  Validation
                       │
                       ▼
                 Application
                       │
                       ▼
                Response Model
                       │
                       ▼
                  HTTP Response
---------------------
HTTP Request
     ↓
Routing
     ↓
Path / Query / Headers
     ↓
Body
     ↓
Pydantic validation
     ↓
Endpoint
     ↓
Business logic
     ↓
Response model
     ↓
Serialization
     ↓
HTTP Response
---------------------
Client
  │
  │ HTTP POST
  ▼
┌───────────────────┐
│      FastAPI      │
└─────────┬─────────┘
          │
          ▼
       Routing
          │
          ▼
   Extraction des paramètres
   ├── Path
   ├── Query
   ├── Headers
   └── Body
          │
          ▼
    Pydantic Validation
          │
     ┌────┴────┐
     │         │
   valide    invalide
     │         │
     ▼         ▼
 Endpoint     422
     │
     ▼
Business Logic
     │
     ▼
Response Model
     │
     ▼
Serialization
     │
     ▼
HTTP Response
--------------------
CLIENT
  │
  │ JSON
  ▼
FASTAPI
  │
  │ validation
  ▼
PYTHON
  │
  │ logique métier
  ▼
PYTHON
  │
  │ serialization
  ▼
JSON
  │
  ▼
CLIENT
-------------------------
1. ROUTING
   ↓
   Est-ce que la requête correspond à une route ?

2. VALIDATION
   ↓
   Les données respectent-elles les types/règles ?

3. LOGIQUE MÉTIER
   ↓
   La demande est-elle autorisée/possible ?
-------------------------------------------
                Produit
                   │
       ┌───────────┼────────────┐
       ↓           ↓            ↓
   Création      Mise à jour   Réponse
       │           │            │
 ProductCreate ProductUpdate ProductResponse
 ProductPatch -> Mise à jour partielle
-------------------------------------------
Le Response Model doit contenir exactement les données que le contrat public de l'API autorise à exposer.
--------
Deux modèles peuvent avoir actuellement les mêmes champs tout en représentant deux contrats différents.
--------
Pour Patch
class ProductPatch(BaseModel):
    name: str | None = None
    price: float | None = None
    quantity: int | None = None

product = find_product(1)
product_patch: ProductPatch
product_path_data = product_patch.model_dump(exclude_unset=True)
for field, value in product_path_data.items():
     product[field] = value
----------
exclude_unset → "Est-ce que le client a envoyé ce champ ?"
exclude_none  → "Est-ce que la valeur de ce champ est None ?"
-----------
0, "", False et [] ne sont pas None
----------
product_path_data.model_fields_set
champ absent
        ↓
model_fields_set ne contient pas le champ

champ = null
        ↓
model_fields_set contient le champ
mais sa valeur est None

champ = ""
        ↓
model_fields_set contient le champ
et sa valeur est ""
----------------------
mode="before"

donnée brute
    ↓
ton validator
    ↓
Pydantic valide/convertit
----------------
mode="after"

donnée brute
    ↓
Pydantic valide/convertit
    ↓
ton validator
----------
